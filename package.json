{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.14.1",
    "@testing-library/react": "^11.2.7",
    "@testing-library/user-event": "^12.8.3",
    "draft-js": "^0.11.7",
    "draftjs-to-html": "^0.9.1",
    "html-react-parser": "^1.4.0",
    "html-to-draftjs": "^1.5.0",
    "immutable": "^3.8.2",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-draft-wysiwyg": "^1.14.7",
    "react-router-dom": "^5.3.0",
    "react-scripts": "^4.0.3",
    "sanitize-html": "^2.5.2",
    "universal-cookie": "^4.0.4",
    "web-vitals": "^1.1.2"
  },
  "devDependencies": {
    "json-server": "^0.15.1"
  },
  "scripts": {
    "start": "react-scripts start & json-server --watch db.json -p 3001",
    "build": "react-scripts build && cat src/service-worker.js >> build/service-worker.js && serve -s build & json-server --watch db.json -p 3001",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
